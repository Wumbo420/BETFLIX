


<!--navBar-->
        <nav>
            <div class="logo">
                <a href="<%= pages_home_path %>">Betflix</a>
            </div>
            <div class="nav-wrapper">
                <ul>
                    <li><a href="<%= pages_home_path %>">Home</a></li>
                    <li><a href="<%= movies_index_path %>">Movies</a></li>
                      <% if user_signed_in? %>
                   <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
                   <% else %>
                   <li><a href="<%= new_user_session_path %>">Login</a></li>
                   <% end %>
                </ul>
            </div>
        </nav>

  <!--End of Navbar-->
